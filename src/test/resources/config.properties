# --- Application Info ---
url=https://qa-refapp.openmrs.org/openmrs/login.htm
browser=chrome
headless=false

# --- Database Config (For DDT) ---
# Use environment variables for sensitive data in production
# Example: set DB_PASSWORD=your_password in your CI/CD pipeline
db.url=jdbc:mysql://localhost:3306/healthcare_test_db
db.user=root
db.password=root

# --- Timeouts ---
implicit.wait=10
explicit.wait=15
page.load.timeout=30
script.timeout=20

# --- Test Configuration ---
test.environment=dev
test.parallel=true
test.thread.count=3

# --- Reporting ---
allure.report.directory=target/allure-results
take.screenshot.on.failure=true